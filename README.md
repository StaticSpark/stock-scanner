# stock-scanner
Command line stock scanner application for stocks traded on Robinhood

# Environment Variables
Environment variables are required to be set for `USER` and `PASS` to retrieve stock quote data from the Robinhood API :
```
USER=yourUser PASS=yourPass babel-node index.js
```
